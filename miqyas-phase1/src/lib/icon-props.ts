import type { LucideProps } from "lucide-react";

/** أيقونات lucide موحّدة — currentColor + strokeWidth ثابت */
export const ICON_PROPS: LucideProps = {
  size: 18,
  strokeWidth: 2,
  "aria-hidden": true,
};
